

dist: trusty
language: java
jdk: oraclejdk8
os: 
  - linux
notifications:
  email:
    - collantes.xavier@gmail.com
addons:
  sources:
    - ubuntu-toolchain-r-test
  apt:
    packages:
      - wget
      - pkg-config
      - bazel
script:
  - bazel run :Hello
